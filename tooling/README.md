# tooling

Repository automation helpers.

Suggested usage:

- `prompts/` reusable prompt fragments and reviewed system prompts
- `scripts/` local automation, indexing, and documentation helpers

Anything that changes delivery behavior should be documented in `docs/runbooks/` or `docs/adr/` before it becomes required process.
