# AI Artifact Review Queue

AI-generated files or outputs that require a human checkpoint should be indexed here before they are treated as final.

Store:

- review requests
- artifact summaries
- approval status
- follow-up fixes

Use the template in `docs/_templates/review-request.md`.
