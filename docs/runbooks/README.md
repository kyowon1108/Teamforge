# Runbooks

Runbooks exist for actions that are risky, stateful, or easy to perform incorrectly.

Typical topics:

- database migrations
- environment setup
- deployment
- rollback
- artifact export/import

Every runbook should contain prechecks, execution steps, verification, rollback, and owner notes.
