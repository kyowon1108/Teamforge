# API Docs

Implementation-facing API decisions belong here.

Use this folder for:

- endpoint groups
- request and response contracts
- auth and permission rules
- websocket event naming
- file upload and validation policy

When a module is implemented, add a focused API doc derived from the template in `docs/_templates/api-spec.md`.
