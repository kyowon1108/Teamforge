# infra

Deployment and environment scaffolding.

Suggested usage:

- `docker/` local containers and Docker assets
- `deploy/` platform-specific deployment notes or manifests

Operational steps that affect live environments should always have a companion runbook in `docs/runbooks/`.
