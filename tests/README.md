# tests

Cross-application verification lives here.

Suggested usage:

- `e2e/` end-to-end flows spanning web and API

App-local unit and integration tests can still live near each app, but anything validating the full kickoff flow should be indexed from this folder.
